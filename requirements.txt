# Aether — v1.0 dependencies (public release)
# Python 3.13+
#
# GPU dependencies (torch + torchaudio) must be installed separately with CUDA:
#   pip install torch==2.7.0+cu128 torchaudio==2.7.0+cu128 \
#       --index-url https://download.pytorch.org/whl/cu128
#
# Optional extras by feature:
#   Local STT + TTS:          faster-whisper, chatterbox-tts (GPU recommended)
#   Cloud voice (ElevenLabs): elevenlabs
#   Avatar:                   configured post-install; see docs/
#   Legacy PySide6 UI:        PySide6 (only needed if running src.desktop_legacy)

# ── Core infrastructure ─────────────────────────────────────
websockets==15.0.1
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
loguru==0.7.2
PyYAML==6.0.2
ruamel.yaml==0.18.10
httpx==0.27.2
aiohttp==3.10.5
numpy==2.1.1

# ── Platform paths + secrets (v1.0 additions) ──────────────
platformdirs==4.3.6
keyring==25.5.0

# ── LLM abstraction (v1.0 addition) ────────────────────────
litellm>=1.52.0

# ── Memory ──────────────────────────────────────────────────
chromadb==1.5.2
rank-bm25==0.2.2

# ── Voice (audio I/O, lightweight — always installed) ─────
sounddevice==0.5.0
soundfile==0.12.1

# Local STT + TTS (faster-whisper, chatterbox-tts, resemble-perth, einops)
# live in requirements-voice.txt because chatterbox-tts 0.1.6 pins numpy<1.26
# which conflicts with the core numpy==2.1.1 pin. Install with:
#   pip install -r requirements.txt -r requirements-voice.txt
# when you want local TTS. Cloud voice (ElevenLabs) and voice.mode=off
# both work without it.

# ── Voice (cloud, optional) ────────────────────────────────
elevenlabs==2.38.0

# ── LLM provider SDKs (optional — litellm uses direct HTTP otherwise) ──
anthropic==0.84.0
google-genai==1.66.0

# ── Testing ─────────────────────────────────────────────────
pytest==9.0.2
pytest-asyncio==1.3.0
