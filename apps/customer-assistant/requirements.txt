# Aether Customer Assistant — Python dependencies for the local demo.
# Python 3.11+ (repo targets 3.13). No API keys, no GPU required.
#
# Install:  pip install -r apps/customer-assistant/requirements.txt
#
# Versions are pinned to match the repo's root requirements.txt so the demo
# shares the same resolved dependency set as the rest of Aether.

fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
PyYAML==6.0.2
httpx==0.28.1

# RAG vector store + bundled CPU embeddings (ONNX all-MiniLM-L6-v2 ships with
# chromadb's DefaultEmbeddingFunction — downloaded + cached on first run,
# no API key). This is the only "heavy" dependency and runs fine on CPU.
chromadb==1.5.2

# Ollama is reached over plain HTTP (httpx) — no client library needed.
# The assistant degrades gracefully when Ollama is not running.
