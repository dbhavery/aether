# apps/

Shippable end-products. Empty in Wave 1.

Planned apps (see `planning/planning/monorepo_plan_draft.md` §1):

- `desktop/` — Tauri shell for OSS Preview and Aether Pro (build-flag gated). X3.
- `guest/` — Cloudflare Worker + Groq endpoint for anonymous trial.
- `docs-site/` — trust center + marketing, renders from `docs/`.
- `mobile/` — React Native companion (reserved; later wave).

No app is scaffolded in Wave 1. Wave 3 lands `apps/desktop/` with the Tauri shell.
