# @aether/ui-kit

**Status:** Wave 1 scaffold — design tokens only. Components land in Wave 3+.

Shared UI primitives. Dark-first, neumorphic monochrome. Consumed by `apps/desktop/`, `apps/docs-site/`, and L7 packages.

## References

- `ARCHITECTURE.md` — the UX principles and the L3 presence / L7 trust surfaces this kit serves.
- `docs/ARCHITECTURE-V2.md` — current architecture detail.

## Wave 1 contents

- `tokens` — placeholder palette, radius, spacing scale.
- `Tokens` TypeScript type.

## Next wave

Wave 3 lands the locked palette (after Don's design research pass), primitive components (`Button`, `Dialog`, `Input`, `Card`), and Framer Motion conventions.
