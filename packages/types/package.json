{
  "name": "@aether/types",
  "version": "0.0.1",
  "private": true,
  "description": "Aether shared TypeScript types — generated from Rust via ts-rs. Wave 1 scaffold contains hand-written placeholders only.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
