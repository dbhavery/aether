{
  "name": "@aether/monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Aether monorepo root — workspace tooling only. No runtime code here.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.10"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
